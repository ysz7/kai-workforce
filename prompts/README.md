# Prompts

Shared, versioned prompt templates. Prompts that belong to one component live
next to it instead - KAI's own prompts are in `application/kai/prompts/`.

Prompts are written in English. The language an agent answers the user in is the
`KAI_RESPONSE_LANGUAGE` setting, never text hard-coded here.
