# Workflows

Predefined processes: a named sequence of steps, each delegated to an employee.
Adding a workflow must not require changing any employee.

The workflow engine arrives in Phase 7; scheduling and event triggers in Phase 12.
